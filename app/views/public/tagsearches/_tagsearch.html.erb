<div>
  <%= form_with url: tagsearches_search_path, method: :get, class:"d-flex justify-content-end", local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.submit'カテゴリー検索' %>
  <% end %>
</div>