<div class="container">
  <div class="row">
    <div class="col">
      <h2>ユーザー一覧</h2>
      <% if @users.exists? %>
        <%= render 'user.index', users: @users %>
      <% else %>
        <p>※ユーザーはまだ登録されていません<p>
      <% end %>
    </div>
  </div>
</div>